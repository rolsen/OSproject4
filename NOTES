the interbench log:
1: vanilla, no applications open on mac
2: slowmo schedule() (empty for loop to 1000), many applications open on mac
3: commented out if (need_resched()) ...  in schedule(), many applications open on mac


timeslice length: run vmstat (context-switches are the cs field)


